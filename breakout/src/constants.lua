-- Global constant variables

DEBUG = true

-- setting window size
WINDOW_WIDTH = 1280
WINDOW_HEIGHT = 720

-- setting virtual window emulated by push.
VIRTUAL_WIDTH = 432
VIRTUAL_HEIGHT = 243

-- initial paddle speed
PADDLE_SPEED = 200

START_CONFIGS = {
    level = 1,
    hearts = 3,
    score = 0
}

-- global high scores table (10 entries)
gHighScoreTable = {}
